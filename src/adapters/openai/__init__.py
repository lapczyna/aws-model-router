"""`OpenAIModelProvider`: the `domain.ports.ModelProvider` implementation for OpenAI's
Chat Completions API (ADR-029), added in Phase 10a as this project's second provider —
proof that ADR-002's provider-independent domain architecture supports a real second
integration without any domain/application-layer change.
"""
