# Scripts

Developer and operational scripts.

Populated starting in Phase 2 with the local CLI route evaluator, and later with
deployment helpers (Phase 5), the opt-in Bedrock smoke-test script (Phase 3), and
load/fault-injection scripts (Phase 9).
